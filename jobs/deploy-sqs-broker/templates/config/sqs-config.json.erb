{
  "log_level": "<%= p('sqs-broker.log_level') %>",
  "username": "<%= p('sqs-broker.username') %>",
  "password": "<%= p('sqs-broker.password') %>",
  "sqs_config": {
    "region": "<%= p('sqs-broker.aws_region') %>",
    "sqs_prefix": "<%= p('sqs-broker.sqs_prefix') %>",
    "allow_user_provision_parameters": <%= p('sqs-broker.allow_user_provision_parameters') %>,
    "allow_user_update_parameters": <%= p('sqs-broker.allow_user_update_parameters') %>,
    "catalog": <%= JSON.dump(p('sqs-broker.catalog')) %>
  }
}

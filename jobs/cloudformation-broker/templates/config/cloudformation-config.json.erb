{
  "log_level": "<%= p('cloudformation-broker.log_level') %>",
  "username": "<%= p('cloudformation-broker.username') %>",
  "password": "<%= p('cloudformation-broker.password') %>",
  "cloudformation_config": {
    "region": "<%= p('cloudformation-broker.aws_region') %>",
    "cloudformation_prefix": "<%= p('cloudformation-broker.cloudformation_prefix') %>",
    "allow_user_provision_parameters": <%= p('cloudformation-broker.allow_user_provision_parameters') %>,
    "allow_user_update_parameters": <%= p('cloudformation-broker.allow_user_update_parameters') %>,
    "catalog": <%= JSON.dump(p('cloudformation-broker.catalog')) %>
  }
}
